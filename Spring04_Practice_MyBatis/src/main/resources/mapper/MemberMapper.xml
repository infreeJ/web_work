<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	
	<select id="getByNum" parameterType="int" resultType="com.example.spring04Practice.dto.MemberDto">
		SELECT num, name, addr
		FROM member
		WHERE num = #{num}
	</select>
	
	<insert id="insert" parameterType="com.example.spring04Practice.dto.MemberDto">
		<selectKey keyProperty="num" resultType="int" order="BEFORE">
			SELECT member_seq.NEXTVAL
			FROM DUAL
		</selectKey>
		INSERT INTO member (num, name, addr)
		VALUES(#{num}, #{name}, #{addr})
	</insert>
	
	<update id="update" parameterType="com.example.spring04Practice.dto.MemberDto">
		UPDATE member
		SET name = #{name}, addr = #{addr}
		WHERE num = #{num}
	</update>
	
	<delete id="deleteByNum" parameterType="int">
		DELETE FROM member
		WHERE num = #{num}
	</delete>
	
	<select id="selectAll" resultType="com.example.spring04Practice.dto.MemberDto">
		SELECT *
		FROM member
		ORDER BY num
	</select>
</mapper>

